
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Home Page</title>
</head>
<body>
    <ul class="nav navbar-nav" style="float: top!important">
        @if (Session["username"] != null)
        {<li> @ViewBag.FullName logged in </li>
         <li>@Html.ActionLink("Logout", "Logout") </li>}

    </ul>
    <div>
        <h1> Welcome to Home Page @ViewBag.FullName </h1>

        <h3> @Html.ActionLink("Employees List", "GetEmployeeList", "Employee")</h3> <br />

        <h3> @Html.ActionLink("Departments List", "GetDepartmentList", "Department")</h3> <br />

        <h3> @Html.ActionLink("Shifts List", "GetShiftList", "Shift")</h3> <br />
    </div>
</body>
</html>
